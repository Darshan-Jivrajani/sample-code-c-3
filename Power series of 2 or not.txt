#include<stdio.h>
int pow2(float );
void main()
{
int i,flag;

printf("Enter the number\n") ;
scanf("%d" ,&i);
flag=pow2(i);
if(flag)
printf("\n %d is power series of 2",i);
else
printf("\n %d is not a power series of 2",i);
}

int pow2(float j)
{
static float x;
x=j/2;
if(x==2)
return 1;
if(x==1)
return 0;
x=pow2(x);
}